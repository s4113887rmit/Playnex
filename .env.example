MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
PORT=3000
# Optional. Without it a random secret is generated at startup, so logins do not
# survive a server restart.
SESSION_SECRET=
