<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<base href="/">
<meta name="description" content="Browse game reviews and ratings on Playnex.">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@500;600;700&family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
<title>Game Reviews — Playnex</title>
<link rel="icon" href="/logo.png">
<link rel="stylesheet" href="/style.css">
</head>
<body>
<a class="skip-to-content" href="#main-content">Skip to main content</a>
<%- include('partials/header', { activeNav: 'community', activeSubnav: 'reviews' }) %>

<main id="main-content">
  <nav class="breadcrumb" aria-label="Breadcrumb">
    <a href="/homepage.html">Home</a><span>/</span>
    <a href="/forum.html">Community</a><span>/</span>
    <span aria-current="page">Reviews</span>
  </nav>

  <div class="page-header"><h1>Game Reviews</h1></div>

  <div class="wrap">
    <div class="blog-toolbar">
      <form class="searchbar" role="search">
        <label for="search" class="visually-hidden">Search games</label>
        <input type="text" name="search" id="search" placeholder="Search games..."
               value="<%= search %>" autocomplete="off">
      </form>

      <div class="blog-toolbar__right">
        <select id="sortSelect" class="sort-select" aria-label="Sort games">
          <option value="">Sort</option>
          <option value="name-asc">Name A-Z</option>
          <option value="rating-desc">Highest rating</option>
          <option value="rating-asc">Lowest rating</option>
        </select>
      </div>
    </div>

    <div class="game" id="gameGrid">
      <% games.forEach(g => { %>
        <a href="/game/<%= g.id %>" class="game-card"
           data-name="<%= g.name.toLowerCase() %>" data-avg="<%= g.avg %>">
          <div class="content">
            <div class="gameimg"><img src="<%= g.image %>" alt="<%= g.name %>" /></div>
            <h2><%= g.name %></h2>
            <div class="star">
              <% for (let i = 1; i <= 5; i++) { %>
                <span class="fa fa-star <%= i <= Math.round(g.avg) ? 'checked' : '' %>"></span>
              <% } %>
            </div>
          </div>
        </a>
      <% }) %>
    </div>
  </div>
</main>

<%- include('partials/footer') %>

<script>
  const searchInput = document.getElementById('search');
  const sortSelect = document.getElementById('sortSelect');
  const grid = document.getElementById('gameGrid');

  function applyClientFilter() {
    const q = searchInput.value.trim().toLowerCase();
    const cards = Array.from(grid.querySelectorAll('.game-card'));

    cards.forEach(card => {
      card.style.display = card.dataset.name.includes(q) ? '' : 'none';
    });

    const sortValue = sortSelect.value;
    if (sortValue) {
      const visible = cards.filter(c => c.style.display !== 'none');
      visible.sort((a, b) => {
        if (sortValue === 'name-asc') return a.dataset.name.localeCompare(b.dataset.name);
        if (sortValue === 'rating-desc') return parseFloat(b.dataset.avg) - parseFloat(a.dataset.avg);
        if (sortValue === 'rating-asc') return parseFloat(a.dataset.avg) - parseFloat(b.dataset.avg);
        return 0;
      });
      visible.forEach(card => grid.appendChild(card));
    }
  }

  searchInput.addEventListener('input', applyClientFilter);
  sortSelect.addEventListener('change', applyClientFilter);
</script>
<script src="/public/js/header.js"></script>
</body>
</html>
